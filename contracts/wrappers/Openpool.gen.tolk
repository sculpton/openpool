// Auto-generated wrapper for contract 'Openpool'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/messages"
import "@contracts/storage"

/// Storage `PoolStorageNotInit` is defined in `@contracts/storage`
struct Openpool {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Openpool.fromStorage(
    storage: PoolStorageNotInit,
    toShard: AddressShardingOptions? = null,
): Openpool {
    val stateInit = ContractState {
        code: build("Openpool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return Openpool { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Openpool.fromAddress(address: address): Openpool {
    return Openpool { address };
}

/// Deploys the contract to the blockchain
fun Openpool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Openpool.sendTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUp { queryId },
    });
    return net.send(from, genericMsg);
}

fun Openpool.sendRefund(
    self,
    from: address,
    queryId: uint64,
    value: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Refund { queryId, value },
    });
    return net.send(from, genericMsg);
}

fun Openpool.sendAddJetton(
    self,
    from: address,
    queryId: uint64,
    comissionPercentMul: uint17,
    minAmount: coins,
    minter: address,
    ownerWallet: address,
    forwardNanoTonLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AddJetton {
            queryId,
            comissionPercentMul,
            minAmount,
            minter,
            ownerWallet,
            forwardNanoTonLimit,
        },
    });
    return net.send(from, genericMsg);
}

fun Openpool.sendRemoveJetton(
    self,
    from: address,
    queryId: uint64,
    minter: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RemoveJetton { queryId, minter },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Openpool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

/// Send an external-in message to the contract with a custom body cell
fun Openpool.sendAnyExternal(self, body: cell): SendResultList? {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}
